{
  "private": true,
  "scripts": {
    "sync-locales": "tsx scripts/sync-locales.ts",
    "update-localization-issue": "tsx scripts/update-localization-issue.ts",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.0.0"
  }
}
